Campo de texto do CRM.

```jsx
<Input size="md" pill placeholder="Buscar por nome, município ou matrícula" />
<Input size="lg" type="email" placeholder="voce@empresa.com.br" />
```

Regra do produto: busca é sempre pílula com o ícone `search` absoluto à esquerda (padding-left 44px).
