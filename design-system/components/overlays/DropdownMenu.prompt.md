Menu suspenso de ações. Itens grandes (peso 700, 16px) porque é tocado com o polegar.

```jsx
<DropdownMenu gatilho={<Button variant="outline" size="icon"><Icon name="move-right" /></Button>}
  itens={ESTAGIOS} onSelecionar={mover} />
```
